<%= render "header" %>

<%= render 'jobs' %>

<%= will_paginate %>

<% if user_signed_in? && current_user.type === 'Employer'%>

<%= link_to 'create job', new_job_path %>

<%= link_to 'my jobs', new_job_path %>

<% end %>
